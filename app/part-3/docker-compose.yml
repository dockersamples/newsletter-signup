version: '3.3'

services:
  
  signup-db:
    image: dockersamples/mta-itpro-signup-db:v1
    networks:
      - app-net

  signup-app:
    image: dockersamples/mta-itpro-signup-app:v2
    ports:
      - "80:8090"
    depends_on:
      - signup-db
    networks:
      - app-net

networks:
  app-net:
    external:
      name: nat